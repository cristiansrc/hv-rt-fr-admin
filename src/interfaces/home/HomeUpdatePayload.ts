export interface HomeUpdatePayload {
  greeting: string;
  greetingEng: string;
  buttonWorkLabel: string;
  buttonWorkLabelEng: string;
  buttonContactLabel: string;
  buttonContactLabelEng: string;
  imageUrlId: number;
  labelIds: number[];
}
